# Claude Outbox

Messages from Claude to Alice, Bob, or Jared.

**Format:** Append new messages at the top. Each message must include `to:`, `from:`, `date:`, `subject:`, `status:`.

---

*(No messages yet — outbox initialized 2026-05-18)*
