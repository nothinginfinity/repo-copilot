name: Unzip and Route Turn Bundle

# DISABLED — workflow_dispatch only.
# This router is for the zip-bundle delivery model (external agents).
# The Perplexity->GitHub flow uses push_files directly and does not need this.
# Full implementation lives in docs/specs/SPEC-001-inbox-routing.md
on:
  workflow_dispatch:

jobs:
  route:
    runs-on: ubuntu-latest
    steps:
      - name: Not yet active
        run: echo "Zip-bundle router is disabled. Trigger manually when zip-bundle delivery is active."
